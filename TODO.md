# TODO

Import features from other ecosystems.

## Python Standard Library — Module Index (3.14.2)

---

## A

- `argparse` — Command-line option and argument parsing library.

## B

- `base64` — RFC 4648: Base16, Base32, Base64, Base85 and Ascii85 encodings.

## C

- `calendar` — Functions for working with calendars (includes `cal`-like features).
- `cmath` — Mathematical functions for complex numbers.
- `cmd` — Build line-oriented command interpreters.
- `codecs` — Encode and decode data and streams.
- `compression` — (category)
- `concurrent` — (category for concurrent.futures and related modules)
- `configparser` — Configuration file parser.
- `csv` — Read and write tabular data to/from delimited files.

## D

- `datetime` — Basic date and time types.

## E

- `email` — Package for parsing, manipulating, and generating email messages.

## F

- `filecmp` — Compare files efficiently.
- `ftplib` — FTP protocol client (requires sockets).

## G

- `getopt` — Portable parser for command line options.
- `glob` — Unix shell-style pathname pattern expansion.
- `graphlib` — Functionality for graph-like structures.

## H

- `hashlib` — Secure hash and message digest algorithms.
- `hmac` — Keyed-Hashing for Message Authentication (HMAC).
- `html` — Helpers for manipulating HTML.
- `http` — HTTP status codes and messages.

## I

- `imaplib` — IMAP4 protocol client.

## J

- `json` — Encode and decode the JSON format.

## L

- `logging` — Flexible event logging system.

## M

- `mailbox` — Manipulate mailboxes in various formats.
- `math` — Mathematical functions.

## O

- `optparse` — Command-line option parsing library.
- `os` — Miscellaneous operating system interfaces.

## P

- `pathlib` — Object-oriented filesystem paths.
- `platform` — Retrieve platform identifying data.
- `poplib` — POP3 protocol client.
- `pprint` — Data pretty printer.

## Q

- `queue` — A synchronized queue class.

## R

- `random` — Pseudo-random number generation.
- `resource` (Unix) — Resource usage info for current process.

## S

- `secrets` — Generate secure random numbers for secrets.
- `signal` — Set handlers for asynchronous events.
- `smtplib` — SMTP protocol client.
- `sqlite3` — DB-API 2.0 implementation using SQLite 3.x.
- `ssl` — TLS/SSL wrapper for socket objects.
- `stat` — Utilities for interpreting os.stat results.
- `statistics` — Mathematical statistics functions.
- `string` — Common string operations.
- `subprocess` — Subprocess management.
- `symtable` — Interface to compiler's symbol tables.
- `sys` — Access to system-specific parameters and functions.
- `sysconfig` — Python configuration information.
- `syslog` (Unix) — Interface to the syslog library.

## T

- `tarfile` — Read and write tar-format archive files.
- `tempfile` — Generate temporary files and directories.
- `termios` (Unix) — POSIX tty control.
- `test` — Regression tests package (testing suite).
- `time` — Time access and conversions.
- `tomllib` — Parse TOML files.
- `turtle` — Educational graphics framework.

## U

- `unittest` — Unit testing framework.
- `urllib` — URL handling modules and utilities.
- `uuid` — UUID objects (RFC 9562).

## W

- `webbrowser` — Controller for web browsers.

## X

- `xml` — XML processing package and modules.
- `xmlrpc` — XML-RPC server and client modules.

## Z

- `zoneinfo` — IANA time zone support.

---
